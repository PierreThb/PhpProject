{% extends "layout.html.twig" %}

{% block body %}
    <div id="divnewuser">
        <p>{{ message }}</p>
        {{ form_start(form) }}
            {{ form_row(form.content) }}
            <button type="submit">Send the request !</button>
        {{ form_end(form) }}
    </div>
{% endblock %}